# 2027 full-year course acceptance report — R2 LF release freeze

## Verdict

**ACCEPTED — the exact LF-normalised local WWHS VET 2027 full-year candidate and its separately frozen connected Head Teacher TAS runtime are accepted for release. No Stage 16 R2 rework is required for these bytes.**

The R2 freeze retains the accepted source-gated annual setup, four terms, 41 weekly control points, 12 gates, ordered dependencies, independent verification, interrupt occurrences, hand-offs, privacy-safe evidence pointers, backup/migration controls and authority-separated Head Teacher TAS wing. It supports both a calm guided path and an experienced-staff full-year view without treating the website as an owner system or proof of compliance.

This verdict accepts the local software candidate. It does not activate unverified 2027 dates, create role delegation, connect a shared register, prove staff access behind an authenticated front door, prove evidence sufficiency or establish school, course or learner compliance.

## R2 frozen acceptance binding

### VET 2027 full-year candidate

- R2 candidate digest: `c6fbb4236e4dcadff317901e80a31ccaaa4af05f7932471cc5261f41809a933f`
- Canonical manifest: `docs/candidate-digest-2027-full-year.sha256`
- Frozen inputs: 9
- Base commit before release: `8c87bfe6b67774ca9670a130f66a53f814fd9808`
- State at Stage 16 R2 completion: **local, staged for prospective release, uncommitted, unpushed and undeployed**

Stage 16 R2 recalculated every manifest-listed raw file hash and independently rebuilt the tab-and-line-feed aggregate. All nine entries matched and reproduced the R2 digest exactly.

### Connected Head Teacher TAS runtime

- R2 connected runtime digest: `32bf370df5ca50c6cc1bdee929c814dc547a6b8503ec8f6a30b46d68e04df8dd`
- Canonical manifest: `docs/connected-head-teacher-runtime-2027-full-year.sha256`
- Frozen inputs: 4

All four connected-runtime entries matched and independently reproduced the R2 runtime digest exactly.

Across both manifests, all 13 text inputs contain zero carriage-return bytes. Independent Git clean-filter checks returned identical raw and path-filtered blob IDs for 13/13 inputs, confirming that the staged LF policy will not silently rewrite the accepted bytes. Any later change to a listed input invalidates the corresponding acceptance.

## LF release-parity correction

Release preflight identified that working-tree line endings could produce different publication bytes from the previously accepted raw-byte freeze. The text inputs were mechanically normalised to LF and `.gitattributes` now declares LF for text and binary handling for PNG/PDF. This changed the byte-level digests and therefore required new Stage 14, Stage 15 and Stage 16 verdicts.

No raw pre-normalisation snapshot was retained, so R2 does not claim a historical byte-for-byte proof that every delta was only an end-of-line byte. Instead, the complete current candidate was independently re-audited on its own exact digests. Fresh functional, migration, rendered and print results reproduce the earlier accepted behaviour and output, with no semantic or rendered regression found. The earlier Stage 16 report remains audit history but is superseded for release-byte acceptance.

## Required independent audit inputs

Both required specialist reports exist, pass and bind to the exact R2 digests:

- `docs/content-audit-report-2027-full-year-r3.md` — **Stage 14 R3 PASS**, VET `c6fbb4…933f`, HT `32bf37…f8dd`;
- `docs/visual-audit-report-2027-full-year-r2.md` — **Stage 15 R2 PASS**, VET `c6fbb4…933f`, HT `32bf37…f8dd`.

Stage 14 R3 reports no open content finding. Stage 15 R2 reports no critical, high, medium or low visual defect requiring correction.

The source map's qualifying-photograph target remains zero and Stage 15 R2 confirms it is justified and met. This is a staff operations workboard, not a student theory package, practical demonstration, presentation or photographic observation task. The student-learning-package gates for theory, videos, assessment rehearsal and 30 photographs are outside this source-approved operations scope rather than missing acceptance items.

## Independent R2 method

Stage 16 R2 operated source-read-only. No VET candidate or Head Teacher TAS runtime file was repaired. The only file created by this stage is this report.

The audit:

- reproduced both manifests with `node qa/candidate-digest.cjs --verify` and an independent raw-file/aggregate calculation;
- confirmed 13/13 Git clean-filter/raw-byte parity and zero carriage-return bytes;
- ran JavaScript syntax checks and the structural validator;
- ran fresh functional and migration/privacy Chromium suites while suppressing report-file writes;
- re-ran the configured-link validator without changing evidence;
- fetched all 13 served candidate/runtime inputs sequentially and matched their response bytes to the manifests;
- exercised 10 risk-selected route families at both 1440 × 900 and exactly 390 × 844, for 20/20 successful route checks; and
- generated all five PDFs in memory, then parsed every page for page count, text, clipping, hidden interactive treatment and runtime errors without writing new PDF files.

The exact-digest Stage 15 R2 evidence supplies the wider visual matrix: all 18 route families at both widths, six keyboard/drawer/dialog journeys and original-resolution inspection of all 16 rendered print pages.

## Structural and operational evidence

### Full-year model — PASS

The fresh structural run reported:

- 176 task instances;
- 59 canonical tasks;
- 48 mapped sources;
- four terms and 41 weeks, distributed 10/10/10/11;
- 12 gates;
- unique task IDs and valid canonical/source references;
- a resolvable, acyclic dependency graph with no reverse term/week edge;
- one independent RTO-update control and one hard-dependent close in every week;
- no stale 2020–2026 operational value outside explicit rollover/history context; and
- the fixed 30 June Term 2 control with its source, independent verification, prerequisites and exception-only late path intact.

All critical source, role, action, done-when and live-verification fields passed. JavaScript syntax passed for both runtimes and all frozen VET data inputs.

### Functional journeys — PASS

A fresh R2 run passed nine of nine scenarios with zero runtime errors:

1. 2027 routes, source status, guided/full modes and all four terms;
2. future-date closure blocking and same-session date refresh;
3. cross-term locking until the prior term closes;
4. missing prerequisite configuration failing closed;
5. verified, not-applicable and independent-verifier guards;
6. critical waiting/exception ownership, chase and escalation;
7. the 30 June control becoming exception-only after the deadline;
8. ordered hand-offs rejecting an unlawful jump; and
9. distinct, reopenable workplace-learning occurrences.

These status controls govern the browser workboard only. They do not decide competency, assessment evidence sufficiency, RTO compliance or an ASQA outcome.

### Backup, migration, reset and privacy — PASS

The fresh R2 migration/privacy run passed six of six scenarios:

- exact full-year restore;
- compatible Term 1 prototype merge;
- compatible 2026 canonical merge;
- byte-stable rejection of malformed and unsupported-build files;
- privacy allowlists and export exclusion of authenticated links, official evidence and personal information; and
- two-step reset clearing only the VET key while preserving Head Teacher TAS and unrelated storage.

The exported backup remains privacy-safe task metadata for continuity, not an RTO submission, evidence repository or shared team database.

## Rendered, destination and print evidence

### Served bytes and route families — PASS

All 13 manifest-listed files returned HTTP 200 from the local candidate and matched their R2 SHA-256 values byte for byte. The risk-selected rendered pass checked the gateway, guided annual cycle, Term 1 compatibility route, Terms 2–4, Systems, Issues, Head Teacher TAS Home and Head Teacher TAS Calendar at laptop and exact-390 widths: **20/20 PASS**, with correct build identity, one H1, containment and no runtime error.

The exact-digest Stage 15 R2 matrix expands that to 36 successful route/viewport checks across:

- gateway, guided cycle, Term 1 compatibility and Terms 2–4;
- Today, Year plan, Workflows, Systems and Issues/handover; and
- all seven Head Teacher TAS routes: Home, Today, Calendar, Teaching, Faculty, People and Reference.

Every route retained one H1, valid navigation state, a skip target, no blank link and no document overflow. Exact-390 client and document widths remained 390 px. VET action targets retained the 44 × 44 CSS-pixel floor.

### Keyboard and focus — PASS

The fresh exact-digest Stage 15 R2 journeys confirm:

- the VET skip link is first in keyboard order and focuses `main-content`;
- both mobile drawers close on Escape and return focus to their triggers; and
- both named task dialogs close on Escape and return focus to the originating control.

No console error, uncaught page error or failed local response occurred.

### Configured destinations — PASS with an activation boundary

The fresh validator found 59 unique embedded HTTPS destinations and 37 declared system links. Every system has an ID, label and destination. No placeholder, non-HTTPS address, blank configured destination or direct protected Google Drive/Docs item identifier is published.

Authenticated front doors are accepted as correctly configured launch routes, not as proof that each authorised staff account currently has access behind them. Signed-in work-account testing remains an activation gate.

### Print package — PASS

All five R2 PDFs were regenerated in memory from the current LF candidate and parsed without writing to the repository:

| Output | Pages | Bytes | Extracted characters | Blank pages | Clipped text items | Result |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| VET Term 1, Week 10 | 3 | 256,127 | 2,085 | 0 | 0 | PASS |
| VET Term 2, Week 10 | 3 | 294,675 | 2,265 | 0 | 0 | PASS |
| VET Term 3, Week 10 | 3 | 294,275 | 2,288 | 0 | 0 | PASS |
| VET Term 4, Week 11 | 2 | 205,183 | 1,896 | 0 | 0 | PASS |
| Head Teacher TAS calendar | 5 | 103,265 | 3,189 | 0 | 0 | PASS |

The page counts, byte counts and extracted-character totals reproduce the preceding accepted print evidence exactly. Print media hid the skip link in all five jobs, emitted no runtime error and retained non-trivial content on all 16 pages. Stage 15 R2 additionally rendered those pages back to pixels and found no crop, overlap, stretch or unreadable reduction.

## Evidence, role and privacy boundary

- No learner name, USI, health/support detail, host/incident detail, assessment evidence, password, credential, private record or controlled assessor package is published.
- Official learner, placement, trainer, assessment, incident, competency and corrective-action evidence remains in its authorised owner system.
- Workboard state is limited to privacy-safe task metadata and a bounded owner-system location, record ID or dated sign-off pointer.
- A checkbox, saved status, export or green card is not represented as proof of competence, evidence sufficiency, compliance or audit outcome.
- Displayed roles become authority only after the current Principal-approved delegation is confirmed.
- The connected Head Teacher TAS wing retains separate browser storage and does not convert TAS completion into VET compliance status.

## Controlled activation conditions

These are explicit authority gates, not software defects:

1. Import and independently verify the published 2027 NESA Timetable of Actions when available.
2. Reconcile each term against current authenticated RTO 90333 guidance and later Hub notices.
3. Reconcile the complete WWHS/Sentral 2027 operational calendar; NSW term windows are not local deadlines.
4. Approve the Principal/delegate role matrix, deputy coverage, access and verifier delegations.
5. Approve a shared authenticated action register or a formal device-local operating decision before browser state is treated as team-wide truth.
6. Test every protected launcher while signed in with the authorised education account and confirm its destination and permission boundary.

Any controlling-source change to a date, dependency, role, evidence destination, escalation rule or closure condition requires the affected candidate and acceptance evidence to be refreshed.

## Release state and conditions

At the instant Stage 16 R2 was completed:

- local exact-digest acceptance: **complete**;
- prospective release files: **staged locally**;
- Git commit of the R2 candidate: **not performed**;
- push to the intended repository: **not performed**;
- deployment/publication: **not performed**;
- cache-busted rendered live verification of these exact bytes: **not performed**.

Steve has separately authorised release after a genuine acceptance PASS. This auditor did not commit, push or deploy. The release pass must re-run both digest helpers immediately before commit, include only the scoped accepted files, confirm staged Git blobs match the raw accepted bytes, wait for deployment, compare public asset bytes with the release revision and cache-bust the gateway, VET term routes, key mobile/keyboard paths and all Head Teacher TAS routes. HTTP 200 or a successful push alone is not live acceptance.

## Final R2 acceptance statement

The exact LF-normalised local WWHS VET 2027 full-year candidate and connected Head Teacher TAS runtime are **ACCEPTED** at the R2 digests recorded above. No candidate or connected-runtime defect requires local rework before the separately authorised release pass. Operational activation remains conditional on current sources, formal delegation, authenticated access and an approved shared-state arrangement.
